<label class="{{message.room}}">
  <span class="msg-timestamp">[{{message.displayTime}}]</span>
  <span class="msg-who">
      {{message.speaker}}@{{message.room}}$
  </span>
  <span class="msg-content">
    {{message.message}}
  </span>

</label>
